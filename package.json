{
  "name": "pr-status-monitor",
  "displayName": "PR Status Monitor",
  "description": "Displays open PR count and status.",
  "version": "0.0.3",
  "license": "MIT",
  "publisher": "ah584d",
  "author": {
    "name": "Avraham Hamu",
    "email": "gibor7@gmail.com"
  },
  "icon": "assets/icon.png",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ah584d/vscode-plugin-pr-status.git"
  },
  "homepage": "https://github.com/ah584d/vscode-plugin-pr-status#readme",
  "engines": {
    "vscode": "^1.103.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "pr-status-monitor.openPrInBrowser",
        "title": "Open PR in Browser"
      }
    ],
    "configuration": {
      "title": "PR Status Monitor",
      "properties": {
        "prStatusMonitor.pollingInterval": {
          "type": "number",
          "default": 2,
          "minimum": 1,
          "maximum": 60,
          "description": "How often to check PR status (in minutes)"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "pnpm run package",
    "compile": "pnpm run check-types && pnpm run lint && node esbuild.js",
    "watch": "npm-run-all -p watch:*",
    "watch:esbuild": "node esbuild.js --watch",
    "watch:tsc": "tsc --noEmit --watch --project tsconfig.json",
    "package": "pnpm run check-types && pnpm run lint && node esbuild.js --production",
    "build": "pnpm dlx @vscode/vsce package --no-dependencies",
    "compile-tests": "tsc -p . --outDir out",
    "watch-tests": "tsc -p . -w --outDir out",
    "pretest": "pnpm run compile-tests && pnpm run compile && pnpm run lint",
    "check-types": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vscode-test",
    "publish:login": "pnpm dlx @vscode/vsce login ah584d",
    "publish": "pnpm dlx @vscode/vsce publish --no-dependencies"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "22.x",
    "@types/vscode": "^1.103.0",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.5.2",
    "@vscode/vsce": "^3.9.2",
    "esbuild": "^0.27.3",
    "eslint": "^9.39.3",
    "npm-run-all": "^4.1.5",
    "sharp": "^0.34.5",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.56.1"
  },
  "dependencies": {
    "@octokit/rest": "^22.0.1"
  }
}
